# useRuneBalance

Hook to get balance of a rune for a given address.

## Usage

```tsx
import { useRuneBalance } from '@midl-xyz/midl-js-react';

function App () {
    const { balance } = useRuneBalance({
        address: 'tb1...address',
        runeId: 'runeId',
    });

    return (
        <div>
            {balance}
        </div>
    );
}
```

## Returns

- **balance**: `string` - Balance of the rune.