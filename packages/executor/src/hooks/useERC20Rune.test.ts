import { describe, it } from "vitest";
import { useERC20Rune } from "~/hooks/useERC20Rune";

describe("useERC20Rune", () => {
  it.skip("should return the correct value", () => {
    // TODO: Implement test
    const test = useERC20Rune("test", { query: { enabled: false } });
  });
});
